{
  "compilerOptions": {
    "noEmitOnError": false, // Позволяет сборку даже при наличии ошибок
    "skipLibCheck": true, // Игнорирует проверку типов в библиотеках
    "strict": false // Отключает строгую проверку типов
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
